{
  "name": "@uaeaf/brand-ui",
  "version": "0.1.0",
  "private": true,
  "description": "The UAEAF identity layer as components (ADR-0098). Built once and read by both the public site and the dashboard: every component learns which surface it is on from the cascade, so none of them takes a background-aware prop. No external dependency — motion is CSS, graphics are inline SVG.",
  "type": "module",
  "exports": {
    ".": "./index.ts",
    "./surfaces.css": "./styles.css"
  },
  "scripts": {
    "lint": "eslint",
    "test": "vitest run"
  },
  "peerDependencies": {
    "next": ">=16",
    "react": ">=19"
  }
}
