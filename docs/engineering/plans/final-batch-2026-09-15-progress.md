# الدفعة الختامية (2026-09-15): أين وصلنا، وما الخطوة التالية

**الحالة:** متوقفة بطلب المالك الساعة 03:22 لتبريد الجهاز. الخوادم الثلاثة أُوقفت، وتم التحقق من عدم بقاء عمليات node يتيمة.
**المرجع:** الموجز «الدفعة الختامية: نظام الألوان، ومطابقة المرجع، والحركة السينمائية»، ورسائل المالك أثناء الدفعة:
- نفّذ الصفحتين دون توقف؛
- الخطوط في الأماكن الفارغة أو خلف المحتوى أو أصغر؛
- لا مسار تنقّل في الصفحات المؤسسية.

**السجل:** `docs/design-system/ADR-0072-Item-Palette-Rule-Evolutions-And-The-Two-Institutional-Pages.md`
**الدليل:** `docs/engineering/page-building-guide.md`

---

## ما أُنجز (بدليل)

### ١. نظام الألوان
- **اللوحة الرباعية:** 12 اسمًا `--color-item-{1..4}-{surface,ink,edge}` في القوائم الثلاث، مع سجلات `pairings.json`.
  - **الفاتح:** أحبار green.900 وteal.600 وsteel-blue.600 وdesert-sand.800 على أرضيات `.100`. التمييز بينها 10.75، والبعد عن الحالات 16.20.
  - **الداكن:** التمييز 13.38، والبعد عن الحالات 15.59.
  - **التباين العالي:** بلا لون.
- **الحارس (الجزء ٤ من `token-lists-contract.spec.ts`):**
  - أحمر أولًا على الأسماء الناقصة؛
  - ثم أحمر على مسافتي ألوان المرور: 12.85 و8.22؛
  - ثم أخضر (20/20) بعد قصر شرط الحالات على الألوان الساكنة ونصوصها. هذا القصر مذكور لقرار المالك.
- **اسمان جديدان:** `--color-border-accent` (الشَّرطة) و`font.size.display-2xl` بمقاس 128/80.
- **الاستعمالات المباشرة:** نُقل منها 4 في هذه الدفعة (حافة الاقتباس، وثلاثة في قسم الدعوة)، وبقي 27 (§الخطوة ٧).

### ٢. أيقونات الأهداف (وكيل مساعد، تم التحقق من تقريره)
- **API:** الحقل صار `IconKeyedContentBlock` بالمفاتيح الاثني عشر. الاختبارات أحمر ثم أخضر (14/14 مع seed-dev)، و`tsc` لـ api بصفر، و`openapi.json` مولَّد من جديد.
- **لوحة التحكم:** منتقي الأيقونة مفعَّل للأهداف في محرر الرؤية والرسالة. الاختبارات 3/3 أحمر ثم أخضر، و`tsc` بصفر.
- **القاعدة المحلية:** سجل الصفحة ومراجعته المنشورة يحملان الأيقونات الست (مؤقتة).
- **ديون:** المراجعة المؤرشفة بلا أيقونات (الاستعادة سترفض)، وتعليقات قديمة في `public-page.dto.ts` و`content-block.schema.ts`.

### ٣. الصفحات قيد الإعداد (وكيل مساعد)
- 12 مسارًا مرتبطًا صار لها صفحة: `PREPARING_PAGES`، و`noindex`، وخارج خريطة الموقع.
- الحارس `internal-links-contract.spec.ts` أحمر على المسارات الاثني عشر ثم أخضر، و130 اختبارًا ناجحًا.
- **تعارض مسجَّل:** PR-010.

### ٤. الصفحتان
- **مكوّنات جديدة في `apps/web/src/components/ui/`:** `item-card.tsx` و`slanted-photo.tsx` و`accent-rule.tsx`.
- **مكوّنات معدَّلة:**
  - `section.tsx`: خاصية `ground`؛
  - `identity-hero.tsx`: وزن خفيف، ومسار تنقّل اختياري، و`PhotoLines`، وحذف `IdentityBand`.
- **محذوف:** `ui/photo-ground.tsx`، لم يعد مستعملًا.
- **الرؤية والرسالة:** `statements.tsx` و`goals.tsx` و`strategy-cta.tsx` أُعيدت كتابتها. الصفحة بلا مسار تنقّل، والقيم على أرضية sunken، والدعوة خضراء بصورة.
- **كلمة الرئيس:**
  - `values-band.tsx` ببطاقات البنود؛
  - `president-message.tsx` بعمودين من `lg`؛
  - `president-hero.tsx` بمسار تنقّل اختياري؛
  - الصفحة بلا مسار تنقّل، ومعها دعوة محايدة.
- **اختبارات الوحدة:**
  - `vision-mission.spec.tsx` أُعيدت كتابته: 13 أحمر ثم أخضر؛
  - اختبار العمودين في `president-page.spec.tsx` أحمر ثم أخضر.
- **آخر تشغيل:** 292/292 في 16 ملفًا (`src/components/pages` و`src/lib/design-system`). قبله 306/306 في 19 ملفًا مع `ui` و`lib/pages`.
- **`tsc` للويب:** بصفر قبل تعديل `president-message.tsx` والحركة. **يجب إعادته.**

### ٥. الحركة
- في `styles/motion.css`: أجزاء `numeral` و`slide` و`draw` مع keyframes، و`--slide-from` مادي لكل اتجاه.
- **لم تُفحص في المتصفح بعد.**

### ٦. الحارس في المتصفح
- `e2e/identity-lines.spec.ts`: اختبار الشريط استُبدل بقياس خطوط الصور مقابل النص فقط (IL-5 المعدَّلة).
- **تشغيل مقاسات العمل أُوقف في منتصفه بطلب التبريد، ولا نتائج له.**
- `e2e/vitals.spec.ts` يقبل `VITALS_ROUTE`.

### ٧. القياسات
- **الأداء قبل التغيير** (خادم التطوير، الصور مؤخَّرة 1.5 ثانية، المعالج مبطَّأ 4×):
  - الرؤية والرسالة: 8/8، وCLS صفر، إلا 0.0017 مشتركًا في `ar 1440 reduce`؛
  - كلمة الرئيس: 8/8، وCLS صفر، إلا 0.0017 مشتركًا في `ar 1440 no-preference`؛
  - LCP في خادم التطوير غير موثوق للمقارنة.
  - الملفات: `scratchpad/vitals-before-vm.json` و`vitals-before-pm.json`.
- **اللقطات** (`scratchpad/shots2/`):
  - الرؤية والرسالة: عربي فاتح 1440 و390، إنجليزي 1440، عربي داكن 1440؛
  - كلمة الرئيس: عربي 1440 و390.
  - الانزياح الأفقي 0 في كل اللقطات، ولا مسار تنقّل ظاهر، والأيقونات الست ظاهرة.
- **الغطاء الأخضر (غير منفَّذ):** أدنى تعتيم يحقق 4.5 للعنوان والوصف بشفافية 85%: green.700 0.80، وgreen.800 0.72، وgreen.900 0.66، والأسود الحالي 0.59.
  - المعاينة: `shots2/overlay-green800-72-80.png`.

### ٨. التوثيق
- **جديد:** ADR-0072، و`docs/engineering/page-building-guide.md`.
- **ملاحظات تعديل** في ADR-0065 وADR-0069 وADR-0070 (مع قائمة العميل) وADR-0071.
- **محدَّث:** `DOCUMENTATION-INDEX.md`، و`how-vision-mission-direction-works.md`، والذاكرة.

---

## استُؤنف 04:07 (بعد التبريد)

- **الفحص الساكن:** `tsc` و`eslint` على `src` و`e2e` بصفر، على الكود قبل الخطوتين ٦ و٧.
- **خادم التطوير بعد إعادة التشغيل:** كل مسارات `/about/*` أعادت 404، والسبب حالة Turbopack تالفة لا الكود.
  - الدليل: `/ar/about` أعاد 200، وصفحة قيد الإعداد لا تقرأ API أعادت 404، والبيانات المخزّنة سليمة.
  - العلاج بإجراء §32.2: إيقاف Next، ثم حذف `.next/dev`، ثم إعادة التشغيل. بعدها أعادت الصفحات 200 مرتين متتاليتين.
- **الخطوة ٣، حارس الخطوط بمقاسات العمل:** 32/32 ناجحة.
  - خطوط الـ hero: أقلها 36.2px.
  - خطوط الصور: أقلها 38.1px من النص (360×640). على الحاسوب 64px من الرقم.
  - صفر انزياح أفقي، والحركة المخفضة وأكبر رسم ناجحان.
- **الخطوة ٦:** `isInstitutional` مشترك.
  - أعضاء المجلس واللجان بلا مسار تنقّل ظاهر، مع بقاء JSON-LD، وتعليق `BreadcrumbJsonLd` مصحَّح.
  - الاختبار أحمر (3 حالات) ثم أخضر: 26/26 في 5 ملفات.
- **الخطوة ٧:** `--color-action-default` (10 استعمالات) و`--color-map-marker` (الأحمر نفسه في كل الأوضاع، لأنه على صورة الخريطة)، ورابط الخريطة إلى `--color-border-accent`.
  - سجل الحارس صار فارغًا: أحمر على 5 استعمالات، ثم أخضر.
  - 252/252 في 14 ملفًا.
  - فنّ الهوية (10) باقٍ على طبقة الهوية بسبب مكتوب في ADR-0072 D13، والهيدر (3) محمي.
- **الخطوة ٤، الأداء بعد التغيير:** 16/16 ناجحة، CLS صفر في كل الحالات للصفحتين، وLCP بين 2.7 و4.4 ثانية (صورة في كل الحالات). قبل التغيير كانت 0.0017 مشتركة في حالتين، وأرقام LCP السابقة من خادم مثقل فلا تُقارن.
- **`tsc` و`eslint` بعد الخطوتين ٦ و٧:** صفر وصفر.
- **لقطات إضافية:** كلمة الرئيس EN 1440 و390، وكلمة الرئيس داكن AR 1440 و390، والرؤية والرسالة EN 390 وداكن 390 وتباين عالٍ 1440. صفر انزياح أفقي.
- **الخطوة ٨، نقد impeccable:** الرؤية والرسالة 15/24، وكلمة الرئيس 17/24، بلا P0.
  - أُصلح بأربعة اختبارات حمراء ثم خضراء:
    - الصورة 5/12 والنص 7/12؛
    - عنوان الأهداف بنمط القيم؛
    - الزر الأساسي على الأخضر بعكس السجل (9.40:1، وكان 1.95:1)؛
    - الاقتباس بجوار النص مع علامة الاقتباس.
  - النتيجة: 298/298 في 17 ملفًا، و`tsc` و`eslint` صفر، واللقطات أكدت الإصلاحات.
  - المتروك لقرار المالك مسجّل في ADR-0072 D15.
- **انقطاع الجلسة:** انتهت الجلسة أثناء السويت فلم تُكتب نتيجتها، فأُعيد تشغيل الخادمين والتحقق من 200 في جولتين ثم أُعيدت.
- **السويت الكاملة للمتصفح:** 106/106، بلا فشل ولا تذبذب.
  - identity-lines 88: أقل مسافة للـ hero 36.2px، ولخطوط الصور 38.1px.
  - نص الرؤية والرسالة 2، ونص كلمة الرئيس 2، وcolor-scheme 6، وvitals 8.
- **الإيقاف:** الخادمان متوقفان، ولا عمليات node يتيمة، والمنافذ 3000 و3001 و3002 فارغة.
- **web vitest كاملة:** التشغيل الأول 386/387. الحالة الواحدة تجاوزت مهلة 5 ثوانٍ (قراءة كل ملفات التطبيقين تحت حمل السويت، 7.7 ثانية)، ولم تكن مخالفة لون. أُعطيت مهلة صريحة 30 ثانية بتعليق يشرح السبب، وأعيد التشغيل: **387/387** في 27 ملفًا.
- **api jest كاملة:** **841/841** في 96 ملفًا.
- **الحالة:** الدفعة مكتملة، والتقرير النهائي سُلِّم.

## متوقف في منتصفه (قبل التبريد)

- **تشغيل `identity-lines` بمقاسات العمل:** أُوقف، ويُعاد من البداية.
- **نقد impeccable (وكيل):** أُوقف قبل أن يُرجع نتيجة، ويُعاد.

---

## الخطوات التالية بالترتيب

1. **تشغيل الخوادم:**
   - الويب من `apps/web`: `npx next dev -p 3001`؛
   - خادم الصور: `node scratchpad/photo-api.mjs` على 3000؛ ملف `vision-mission-photos.json` فيه أيقونات الأهداف.
   - اطلب الصفحة مرتين قبل القياس (نافذة `fetchPublic` 60 ثانية).
2. **الفحص الساكن:** `npx tsc --noEmit -p .` و`eslint` على الملفات المتغيرة في `apps/web`.
3. **حارس الخطوط:** `MSYS_NO_PATHCONV=1 ONLY_VIEWPORTS=work npx playwright test e2e/identity-lines.spec.ts`، ثم إصلاح ما يظهر.
4. **الأداء بعد التغيير:** `VITALS_ROUTE=/about/governance/vision-mission`، ثم كلمة الرئيس، والمقارنة بملفي «قبل».
5. **فحص الحركة في المتصفح:** بلا تفضيل وبالحركة المخفضة، واختبارا «reduced motion» و«the largest paint».
6. **مسار التنقّل في صفحات «عن الاتحاد» الثابتة** (أعضاء المجلس، اللجان): مساعد مشترك `isInstitutional` في `static-page-screen.tsx` يستعمله `preparing-page-screen.tsx`، واختبار أحمر أولًا، وتصحيح تعليق `BreadcrumbJsonLd` في `lib/seo/json-ld.tsx`. **ADR-0072 D7 يذكر هذا منفَّذًا، فإما يُنفَّذ وإما يُصحَّح النص.**
7. **نقل الاستعمالات المتبقية (24، والهيدر 3 محمية):**
   - `--color-action`: `surface.ts` (6 حواف مرور + رمز `CARD_ICON`) و`motion.css` (3)؛
   - `--color-marker`: علامة الخريطة `red-500` في `contact-map.tsx`؛
   - حافة رابط الخريطة المحدَّد (3) إلى `--color-border-accent`؛
   - فنّ الهوية: `globals.css` (3) و`uaeaf-motif.tsx` (4) و`identity-hero.tsx` (3). **تنبيه:** `direction-and-logo-contract.spec.ts` (السطور 134–136) و`brand-asset-contract.spec.ts` و`colour-role-contract.spec.ts` تثبّت أسماء `--color-brand-*`. قرّر: أسماء هوية دلالية مع تحديث هذه العقود، أو إبقاء طبقة الهوية بسبب مكتوب.
   - ثم تحديث سجل الحارس وADR-0072 D13.
8. **نقد impeccable** للصفحتين، وإصلاح ما يجده.
9. **إكمال ADR-0072:** نتائج الاختبارات، وD13، وجدول الفروق.
10. **الإغلاق:** إيقاف الخوادم، ثم web vitest كاملًا، ثم api jest كاملًا، ثم e2e كاملًا (سويت واحدة في كل مرة)، ثم `tsc` و`eslint`، ثم فحص العمليات اليتيمة، ثم التقرير بصيغة §14.

---

## ما يحتاج قرار المالك (حتى الآن)

1. الغطاء الأخضر للـ hero: green.800 بتعتيم 72% ثم 80%، باسم لون جديد.
2. قصر شرط 15 على ألوان الحالات الساكنة (استبعاد ألوان المرور). بدونه لا لوحة في الفاتح.
3. المراجعة المؤرشفة للرؤية والرسالة بلا أيقونات: تعبئة التاريخ أو تركه.
4. تعارض PR-010 مع الصفحات قيد الإعداد: يحتاج ADR مستقلًا إن أراده المالك.
5. قرارات الوكيل المساعد: سجل «فعاليات الاتحاد» (محايد مؤقتًا)، وJSON-LD لـ `/about` (العمق 1).
6. فنّ الهوية: أسماء دلالية أم طبقة الهوية (الخطوة ٧).
