import { sponsorsSectionHandler } from "@/lib/admin/sponsor-relations/relation-routes";

/** The SPONSORS section's banner preference and call to action, on a SPONSORS section only. */
export const PATCH = sponsorsSectionHandler;
